# Changelog

## 4.1.0

- Added the `virtualizer` prop to configure estimated row size, gap, viewport height, and dynamic measurement.
- Added dynamic measurement support through TanStack Virtual's `measureElement` for variable-height options.
